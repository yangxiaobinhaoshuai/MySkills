# MySkills

Personal Codex skills for terminal configuration and related workflows.

## Skills

- `wezterm-config`: edits WezTerm configuration in `~/.wezterm.lua`.
- `ghostty-config`: edits Ghostty configuration files and looks up options from the official Ghostty docs.

## Usage

Copy a skill directory into your Codex skills directory:

```sh
cp -R ghostty-config ~/.codex/skills/
```

Then invoke it by name, for example:

```text
Use $ghostty-config to make Ghostty transparent.
```
